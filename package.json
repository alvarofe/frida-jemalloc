{
  "name": "jemalloc_frida",
  "version": "0.0.1",
  "description": "JEMALLOC parser for frida",
  "private": true,
  "main": "agent/index.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile agent/index.ts -o _agent.js",
    "watch": "frida-compile agent/index.ts -o _agent.js -w",
    "execute" : "python load_frida_script.py 23cc4c003a0c7ece _agent.js"
  },
  "devDependencies": {
    "@types/frida-gum": "^14.1.0",
    "@types/node": "^12.7.2",
    "frida-compile": "^9.0.8"
  }
}
